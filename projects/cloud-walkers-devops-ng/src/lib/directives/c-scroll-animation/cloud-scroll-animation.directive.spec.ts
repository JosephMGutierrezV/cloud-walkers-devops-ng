/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CScrollAnimationDirective } from './cloud-scroll-animation.directive';

describe('Directive: CScrollAnimation', () => {
  it('should create an instance', () => {
    const directive = new CScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
